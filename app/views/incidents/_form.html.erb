<%= form_for(@incident) do |f| %>
  <% if @incident.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incident.errors.count, "error") %> prohibited this incident from being saved:</h2>

      <ul>
      <% @incident.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver_history_id %><br>
    <%= f.text_field :driver_history_id %>
  </div>
  <div class="field">
    <%= f.label :date_of_incident %><br>
    <%= f.date_select :date_of_incident %>
  </div>
  <div class="field">
    <%= f.label :claim_value %><br>
    <%= f.number_field :claim_value %>
  </div>
  <div class="field">
    <%= f.label :incident_type %><br>
    <%= f.text_field :incident_type %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
